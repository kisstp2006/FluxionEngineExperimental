#pragma once
// This header is intentionally minimal.
// The game project should NOT include this — it only needs Application.h.
// It exists so EntryPoint.cpp can declare what it needs.
#include "Application.h"
